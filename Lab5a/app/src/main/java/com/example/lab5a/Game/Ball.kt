package com.example.lab5a.Game

import android.graphics.Paint

class Ball(private val startX: Float, private val startY: Float,
           private val startWidth: Float, private val startHeight: Float, color: Paint):
    GameObject(startX, startY, startWidth, startHeight, color) {

    }